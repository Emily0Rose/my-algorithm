#include <bits/stdc++.h>
using namespace std;

//this program is thought be used on AtCoder

//this is the original algorithm which calculates a sum of digits written in base 10

int main() {
  int x; //これから調べるもの
  cin >> x;
  
  int count = 0;
  for(int i = 0; i < 5; i++) {
    //五桁の数を調べる場合
    count = x % 10; //count from the number on the laft side
    x /= 10; //dismiss the number just ahve been counted 
  }
}
